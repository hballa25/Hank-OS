# The Forge — build log 2026-07-07

- Grilled Hank (grill-me, 4 rounds) into the automation-spine design; resolved the Round 2/3 approval contradiction → money-only human gate + automated quality gates.
- Wrote `90 System/automation-spine.md` (blueprint).
- Wrote agent specs `agents/forge.md` (orchestrator) + `agents/workbook-forge.md` (Phase 1 worker).
- Created `90 System/approvals/` feed (README + schema).
- Updated `index.md`.
- Built + verified Phase-1 dashboard slice: `Automations` tab (`src/components/AutomationsTab.jsx`), server endpoints `/api/forge/jobs`, `/api/forge/run`, `/api/approvals`, `/api/approvals/decision` (`server/index.js`), styles in `styles.css`. Verified in browser preview: job launcher renders (Workbook active, others planned), approval inbox renders money-gate items, Approve/Reject writes status back to frontmatter, no console errors.
- Restarted the always-on hidden dashboard with the new code.
- Next: wire `/api/forge/run` to a real ruflo swarm (needs `ruflo init` in repo + workbook preset + Gemini bulk key); route approvals into The Briefing + PWA push; scheduler for autonomous runs; App Copilot (Phase 2).

## ruflo swarm executor wired (later 2026-07-07)
- Installed/contained ruflo in `.forge-runtime/` (gitignored); configured Google/Gemini provider (key from ~/.vibe-trading/.env, not echoed) — connectivity test PASSED; ran `hive-mind init`.
- Confirmed execution model: ruflo coordinates, agents run via the local `claude` CLI (subscription — no Anthropic API key/cost). Gemini = bulk tier.
- `/api/forge/run` now takes `executor: claude|swarm`; swarm = `ruflo hive-mind spawn --claude -n 4 -o <objective>` (visible terminal, first-run-watch). Automations tab has an Engine toggle. Verified param validation returns 400 without spawning.
- Known: ruflo prints benign config-load warnings + one libuv shutdown assertion on Windows; test runs still exited 0. First real workbook swarm run should be watched before trusting headless.

## First Workbook run + ruflo ENOENT fix (evening 2026-07-07)
- Grill-first: grilled Hank into Book 5 seed note (`20 Hustles/AI Workbooks/Book 5 - The AI Homework and Feedback Agent.md`); updated workbook-forge spec to two-phase (grill → seed → forge).
- First swarm fire: coordinator built the correct objective, but ruflo workers failed `spawn claude ENOENT` (Windows: ruflo spawns bare `claude` without shell; only claude.cmd was on PATH).
- FIX: hardlinked `claude.exe` into `~/AppData/Roaming/npm/` (on PATH). Verified with `spawnSync('claude',['--version'],{shell:false})` → returns version, no ENOENT. Re-fired swarm; a claude worker spawned cleanly.
- Also: `/api/forge/run` solo executor now passes `--dangerously-skip-permissions` so runs don't stall on file-write prompts (reversible writes auto-approved; money gate unchanged).

## Book 5 workbook forge — completed run (2026-07-07 ~21:00)
- Ran `workbook` job on `Book 5 - The AI Homework and Feedback Agent.md` seed + 1-hop context (AI Workbooks hub, Book 3, TaiGrader, XPScholar).
- Dispatched swarm: researcher (market/positioning/pricing) + page writer A (§1–4) + page writer B (§5–8) + listing writer — parallel, returned to assembler.
- Assembler quality gate PASSED: coherence, grade-fit, brand voice, **no student-identifiable data** (all examples fake "Student A"). Reconciled cross-writer page-number + pricing conflicts.
- Wrote 3 drafts: `Book 5 — Workbook Draft v1.md` (~18pp body), `Book 5 — Listing Pack.md` (TPT+Etsy), `Book 5 — Thumbnail Brief.md`.
- Updated product note: `status: idea → building`, `assets:` linked to the 3 drafts, added Forge-run summary + 3 open decisions (title/price/featured-tool) + next actions.
- Wrote approval item `approvals/2026-07-07-2100 workbook book-5.md` (gate: review — no money spent).
- Guardrails honored: no money spent (pricing is recommendation only); no student data in any cloud call. Canva/PDF export left as reviewed next-step, not auto-fired.

## ✅ First successful end-to-end run (Book 5, Opus 4.8)
- Root cause of all earlier stalls: `claude` CLI defaulted to Fable 5, which hit its monthly spend limit. Not a code bug.
- Fix: switched `~/.claude/settings.json` model → claude-sonnet-5, then → claude-opus-4-8 (Hank's pick). Verified plain `claude -p` runs uncapped.
- Executor final form: headless `claude -p` via stdin (shell:true) so PATH resolves + no argv mangling; `--dangerously-skip-permissions` so it doesn't stall.
- Run produced (all `#draft`): `Book 5 — Workbook Draft v1.md` (571 lines / 8 sections, real prompt library + fill-in + 2 starter projects + safety page, reviewer-rated ready-for-layout), `Book 5 — Listing Pack.md` (TPT+Etsy copy), `Book 5 — Thumbnail Brief.md`, and approval item `approvals/2026-07-07-2100 workbook book-5.md` (gate: review). Seed note status → building. No money, no student data.
- Pipeline proven: grill → seed note → Forge (Opus) → drafts + listing + thumbnail + approval-inbox item.
